import { TipoNegocioPipe } from './tipo-negocio.pipe';

describe('TipoNegocioPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoNegocioPipe();
    expect(pipe).toBeTruthy();
  });
});
